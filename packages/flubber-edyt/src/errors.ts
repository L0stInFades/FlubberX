export const INVALID_INPUT =
  "All shapes must be supplied as arrays of [x, y] points or an SVG path string.";

export const INVALID_INPUT_ALL =
  "flubber.all() expects two arrays of equal length as arguments. Each element in both arrays should be an array of [x, y] points or an SVG path string.";
